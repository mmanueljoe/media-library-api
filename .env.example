# Copy this file to .env.development (or .env.test) and fill in real values.
# In production these are set via the hosting dashboard, not a file.

NODE_ENV=
PORT=
MONGO_URI=
JWT_SECRET=
JWT_EXPIRES_IN=
LOG_LEVEL=
